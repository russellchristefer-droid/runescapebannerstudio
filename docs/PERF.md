# Perf budgets

- `/legal` JS: stay without canvas / gazetteer / Three.js.
- `/` first JS: studio lazy; Hiscores on Look up.
- Drag: ref + rAF only; React state on pointerup.
- Still cache: one canvas, export pixels not CSS×DPR.
- Cards: lazy + one retry then umber. `content-visibility` on the tile, not a coloured wash.
- Chat Bold is plate-only. Do not preload it on Legal.
- Hero: current + next only. Cycle 2:03. Street lines stay five minutes.
- Production: no source maps. `ci:gates` still runs.
- Scroll restoration on inner routes.
