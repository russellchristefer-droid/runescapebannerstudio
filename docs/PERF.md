# Perf budgets

- `/legal` JS: stay without canvas / gazetteer / Three.js.
- `/` first JS: studio lazy; Hiscores on Look up.
- Drag: ref + rAF only; React state on pointerup.
- Still cache: one canvas, export pixels not CSS×DPR.
- Cards: lazy + one retry then umber.
- Scroll restoration on inner routes.
