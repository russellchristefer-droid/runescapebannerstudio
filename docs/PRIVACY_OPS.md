# Privacy ops

Collected: host edge logs, optional Cloudflare bot cookie, localStorage `rs-banner-studio` and `rs-hiscore-name`, mail drafts the user opens themselves.

Not collected: Jagex passwords, cards, GPS.

Retention: host logs 14–30 days unless a takedown needs longer.

No session replay. No marketing pixel.

Clear saved desk on `/legal` deletes the two keys above.
