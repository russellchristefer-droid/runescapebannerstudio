# SLOs

- `/` HTML 200 as often as the host allows. Fan desk, best effort.
- Export p95 under 2s after the still is local on a mid phone.
- Hiscores die → `boards busy`. Download stays on.
- Export break is Sev-1. Fix before new towns.

Kill switches (server env):

- `HISCORES_ENABLED=false`
- `WOM_ENABLED=false`
- `UPLOADS_ENABLED=false`

Desk must still paint a typed name on a hosted still and export.
